name: pages
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # No build: the dashboard self-discovers the registry at runtime. Just publish the static page
      # alongside the registry + docs it fetches, with a directory listing of the skills.
      - name: assemble the site
        run: |
          mkdir -p _site/docs
          cp docs/site/index.html _site/index.html
          cp -r skills _site/skills
          cp cyberware.md _site/cyberware.md
          cp docs/*.md _site/docs/
          python3 -c "import os,json; json.dump(sorted(d for d in os.listdir('skills') if os.path.isdir(os.path.join('skills',d))), open('_site/skills.json','w'))"
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
